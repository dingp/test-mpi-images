name: Build Ubuntu 24.04 CUDA 13.2.0 cuDNN image

on:
  push:
    branches: [ main ]
    tags:
      - "v*"
    paths:
      - "container/ubuntu-24.04-cuda-13.2.0-cudnn.Dockerfile"
      - ".github/workflows/build-ubuntu-24.04-cuda-13.2.0-cudnn.yaml"
  workflow_dispatch:

jobs:
  build_and_push_image:
    name: Build and push image
    runs-on: ubuntu-latest
    permissions:
      packages: write
      contents: read
    steps:
      - name: Check out the repo
        uses: actions/checkout@v6

      - name: Log in to the Container registry
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v6
        with:
          images: ghcr.io/dingp/test-mpi-images
          tags: |
            type=raw,value=ubuntu-24.04-cuda-13.2.0-cudnn

      - name: Build and push image
        uses: docker/build-push-action@v7
        with:
          context: .
          file: ./container/ubuntu-24.04-cuda-13.2.0-cudnn.Dockerfile
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
